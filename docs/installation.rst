============
Installation
============

At the command line::

    $ easy_install django-dhcp

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-dhcp
    $ pip install django-dhcp